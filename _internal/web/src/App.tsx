// Placeholder — full implementation added in the components commit.
// This file exists so the scaffold can be verified as a working build
// before the UI components are written.
export function App() {
  return (
    <div className="min-h-screen bg-slate-900 text-slate-100 flex items-center justify-center">
      <p className="text-slate-400">Loading Practice Tracks…</p>
    </div>
  );
}
